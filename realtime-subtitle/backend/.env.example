# 服务端口
WS_PORT=8765

# STT：mock（测试）| whisper（需 pip install openai-whisper）
STT_PROVIDER=mock

# 翻译：mock | google（免费）| deepl（需 API Key）
TRANSLATE_PROVIDER=google

# DeepL（仅当 TRANSLATE_PROVIDER=deepl 时）
# DEEPL_API_KEY=your_key
