fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-dotenv>=1.0.0
websockets>=12.0
python-multipart>=0.0.6

# 翻译（可选，不装则用 Mock）
googletrans==4.0.0-rc1

# STT 真实语音识别（二选一安装）
# pip install openai-whisper   # 本地，需约 1GB 磁盘
# pip install faster-whisper   # 更轻量
